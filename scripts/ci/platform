#!/usr/bin/env bash
set -euo pipefail

scripts/ci/configure debug -DLIGHT_BUILD_TESTS=ON -DLIGHT_BUILD_BENCHMARKS=OFF
cmake --build --preset debug
scripts/ci/test debug
./build/debug/light --version | grep -q '^light '
